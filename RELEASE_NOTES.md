# v1.0.1

界面小改版：

- 主界面改成分组卡片和圆角输入框，修正数字输入挤在下划线上的问题。
- 使用类似 Material You 的动态主题色，优先跟随系统/壁纸取色。
- 权限入口移动到独立页面，主界面按钮层级更清楚。
- 悬浮控制面板和选区按钮统一为更柔和的圆角样式。
- 辅助功能状态判断更准确。

# v1.0.0

首个版本：

- 支持固定次数和无限点击。
- 支持点击间隔设置。
- 支持矩形范围和中心点半径范围。
- 支持范围内随机点击。
- 支持随机时间浮动。
- 支持悬浮控制面板和全屏范围选择层。
- 使用 Android 辅助功能服务执行点击。

APK 文件：`lian-dian-qi-v1.0.1-release.apk`
